import 'package:flutter/material.dart';

class AddBabyScreen extends StatelessWidget {
  const AddBabyScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("AddBabyScreen"),
    );
  }
}
